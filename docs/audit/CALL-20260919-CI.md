# CI regression attribution — 19 September 2026

Run `35472247190` reported these failures in jobs `105975199614` (web inventory) and `105975199602` (offline audit/consent/adoption).

| Failure | Attribution | Repair |
| --- | --- | --- |
| Four reflection API methods missing from web inventory | Introduced by this candidate. Base `7bcc624` passes with 53 methods/54 declarations. | Add one explicitly **planned** reflection-editing capability group, without claiming browser parity. Candidate now inventories 57 methods/58 declarations. |
| Consent launch fixture count is 9 rather than 8 | Introduced by the new `CaptureRecoveryTests.swift`. The original base check passes. | Review all nine fixtures by filename instead of only counting them; additionally require the offline capture fixture to keep cloud AI consent declined. |
| Extracted AppModel cannot resolve `markSpaceTypeOutOfSync` | Pre-existing missing harness dependency: the base test is byte-identical, and both the extracted caller and actual helper bodies are unchanged from `7bcc624`. | Add an inert business-type preference/notification dependency to the test scaffold. Adoption product logic and the existing behavioral/negative assertions remain unchanged. |

Completed checks: candidate web foundation 14/14; all three injected faults (`missing-upload`, `low-contrast`, `no-op-validator`) exit 1 as required; candidate consent suite 5/5, including actual isolated Foundation persistence; base web foundation 14/14 and the base consent fixture check pass. The repaired adoption harness passes both Node tests and 73 actual local-binding/persistence assertions. All three behavioral mutants compile and then fail assertions with exit 1, as required. No skips, assertion removals, or timeout increases were introduced.

Source comparison receipt: `/tmp/rendprop-ci-baseline-comparison.json`. Executed adoption receipt and compile/mutant logs: `/var/folders/j3/n4p7jg5x5lv35xgcv9hw9yx80000gn/T/rendprop-local-binding-swift-nIizBc/receipt.json`. The receipt includes the actual source and extracted-scaffold hashes. The targeted command was `node --test tests/phase1/adoption-local-bindings.test.mjs`.

The first full baseline Phase 1 execution ran during heavy simulator/host pressure and timed out in Swift compiler subprocesses. This is **not** reported as an executed reproduction of the missing-member diagnostic. The CI diagnostic plus identical baseline harness/caller/helper establishes that attribution. The corrected executable adoption harness subsequently passed after the concurrent capture UI run exited, under its normal limits and with its existing negative controls. The complete Phase 1 glob was not rerun locally after repair; fresh CI remains the full-job verdict.


Independent photo-fallback review executed the complete `ai-photo` handler and actual router/chain against mocked database, auth, quota and provider boundaries. With an empty route table and the router flag off, request bodies `{ "edit": "constructor", "space_type": "real_estate", "image_b64": "synthetic-only" }` and the same body with `"edit": "__proto__"` each returned **200, two quota debits, one provider attempt and no refund** on both `7bcc624` and the frozen pre-fix candidate. Inherited-key acceptance therefore pre-existed; the candidate's six-task-only predicate left a bypass in its new active-route requirement. Ordinary `unsupported` and `tostring` inputs already rejected with 400 and no quota/provider activity.

The independently executed repaired handler rejects all four invalid names with **400, zero quota debits, zero refunds and zero provider attempts**. Supported `sky` with no eligible route returns **503, two quota debits, two refunds and zero provider attempts**. The repair validates edit modes before quota and makes the active-route requirement apply to the complete `photo.*` namespace. The final migration changes only the fallback marker on already-enabled eligible rows; it does not enable disabled rows or alter flags, models or prices. Non-photo legacy routing remains outside this change.

Independent executable evidence: `/var/folders/j3/n4p7jg5x5lv35xgcv9hw9yx80000gn/T/rendprop-photo-route-proof-auz65vvv/baseline-receipt.json` (34 checks), `fixed-receipt.json` (18 checks), and adjacent `proof.mjs` plus frozen source snapshots. Run `node /var/folders/j3/n4p7jg5x5lv35xgcv9hw9yx80000gn/T/rendprop-photo-route-proof-auz65vvv/proof.mjs` for baseline/candidate and append `--fixed` for working-tree repair. Each receipt contains SHA-256 hashes of the complete handler, router and chain sources. The verified repaired hashes are respectively `3d0cfea20b017b83fa5ee4dbc0322d29a59d462e5912369cafd9ab914d45f72c`, `c79cdb9fc0f357b82f1593493528a98556212ab4b4dff8e6b28ac024bf6c6a3d`, and `d29cbdae97fbcf9f5c4a54dc80dc1bbcf0051e06439206befceba698ccda0eb0`. All provider attempts were synthetic boundary calls; no external request or paid operation occurred.

The committed regression entrypoint is `services/supabase/functions/_shared/photo_route_fallback_test.ts`, which executes the real handler/router/chain and covers both router flag states, inherited and unknown edit names, disabled/missing photo routes and quota refunds. It is intentionally the single canonical regression rather than a duplicate committed VM runner. The server agent's full-suite and native database evidence is recorded in [CALL-20260919-CI-DATABASE.md](CALL-20260919-CI-DATABASE.md). No remaining blocker was found in this bounded independent review.
