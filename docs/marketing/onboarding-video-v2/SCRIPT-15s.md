# Rendprop — onboarding video v2, 15-second cut-down

Three beats from the SAME recording as `SCRIPT.md` — the absolute minimum
that still shows the hook, the product, and that it isn't just for homes.
Build with `--only-scripted` against the same `tour-raw.mp4` / `marks.txt`.
See `docs/marketing/onboarding-video-v2/README.md` for the exact command.

No hooks, no title card. The CTA card is trimmed to 2 s (`--end-card-seconds
2`) — the full-cut's 3 s reads slow at this length.

---

## 00 · 0:00–0:03 · Cold open

On screen: Cold launch → Home → "Watch the sample tour" → the hosted demo
listing page flies as you drag.

Say: Every listing looks the same. Until yours flies.

Caption: Until yours flies

## 01 · 0:03–0:07 · This is Rendprop

On screen: Home hero — "Win the listing. Skip the film crew."

Say: This is Rendprop — a phone walkthrough becomes a tour you can send.

Caption: One walkthrough. One tour. One link.

## 12 · 0:07–0:11 · Six kinds of space

On screen: The business-type menu — pick "Event venue," Home re-themes,
switch back to "Real estate."

Say: It works for homes, venues, restaurants, retail, or gyms.

Caption: Six kinds of space, six different apps

---

## Cards (generated by the build)

- CTA card, 2 s — `--end-card "Try it out now — free for 7 days" --end-card-seconds 2`
- Brand card, 2 s — the build's own defaults
