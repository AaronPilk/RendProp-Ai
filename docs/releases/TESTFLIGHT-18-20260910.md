# Internal TestFlight 1.0 (18): independent delivery evidence

**Available** in the existing internal group as of 2026-09-10 21:23 UTC.
Apple reports VALID / INTERNAL_ONLY / IN_BETA_TESTING, iOS16 minimum, not expired.
App Store1.0 is still WAITING_FOR_REVIEW with build16 attached. No App Store
metadata, prices, attachment or submission was changed by this Codex run.

Source: `ed0131b03e83f01ba60c55f209bd960bb9278dfc`, tree
`6a893ebd2a1de3264dd1d5768d8b733b3295858c`, clean release worktree.
The new bounded homogeneous-row pose check and both hardening rounds are included.
This is **local capture Phase A**, not a deployed reconstruction/3D-tour product.

## Who uploaded, and when the tests ran

Claude's bridge command1529 archived/uploaded build18 while this Codex run executed
the fresh integrated UI suite. Its log names ed0131b; the owner confirmed the source
and archive location. Its upload-success log is17:05:45 Eastern; Apple's uploadedDate
is17:06:25 Eastern. These times differ by40 seconds, not by three hours.

Our own `distribute.py archive` then correctly exited1 on changed live build inventory,
before creating an archive-attempt marker. There was no duplicate upload, no bypass,
and no automatic build19. Do not rerun that archive/upload helper or rewrite its
baseline to force a pass.

**Five fresh UI tests subsequently passed on ed0131b, zero skips**, with22 required
screens individually inspected. This is retrospective same-source verification;
it was NOT the pre-upload UI gate for Claude's upload. Historical build17 results
were not substituted. This ordering exception is recorded rather than hidden.

## Exact commands and outcomes

Run in `/Users/pilksclaes/Rendprop AI/spatial-hardening-20260910` unless noted.

```sh
PYTHONDONTWRITEBYTECODE=1 python3 /tmp/rendprop-spatial-release18.pVLudX/build_test.py
# exit0; fresh Release build-for-testing + Reviewer1/Main1/Spatial3; 22 required screens

PYTHONDONTWRITEBYTECODE=1 python3 /tmp/rendprop-spatial-release18.pVLudX/guard_tests.py
# exit0; 20 offline helper tests; no Apple calls

python3 tools/spatial-spike/verify_app_bundle.py \
  /tmp/rendprop-b18.76310/Rendprop-1.0-18.xcarchive/Products/Applications/Rendprop.app \
  --build 18 --lab
# exit0; exact version/build/resource/compatibility/capture-lab gate

codesign --verify --deep --strict \
  /tmp/rendprop-b18.76310/Rendprop-1.0-18.xcarchive/Products/Applications/Rendprop.app
# exit0; archived app only, not independently recovered distribution IPA

strings /tmp/rendprop-b18.76310/Rendprop-1.0-18.xcarchive/Products/Applications/Rendprop.app/Rendprop \
  | rg -F 'Invalid c2w homogeneous row (max error'
# exit0; bounded diagnostic present in actual executable

PYTHONDONTWRITEBYTECODE=1 python3 /tmp/rendprop-spatial-release18.pVLudX/postflight.py
# exit0; exact Apple internal readiness and unchanged pending attachment
```

The source-bound UI receipt records the full Xcode command arrays, project/app/runner
hashes and xcresult hashes. Tests target synthetic simulator
`D4BAC4B1-5F7D-4C4E-88A5-FC10746C152C`, Release scheme
`RendpropSpatialTestFlight`, with no parallel testing. Required suites are
`ReviewerWalk/testReviewerWalk`, `RendpropUITests/testWalk`, and the three
`SpatialCaptureIntegrationTests`. No real account was deleted or provider tested.

## Durable hashes, honest limits

`testflight-18-receipt.json` preserves all11 archived app-file SHA256 values,
archive Info.plist/export-options digests, executable UUID, source identifiers,
test receipt hashes and Apple state. JSON manifest hashing is explicitly specified;
it differs from Claude's aggregate manifest string format, so compare individual
file digests or use the documented encoding—not an unspecified aggregate hash.

Archive source association is supported by the clean worktree log, owner confirmation,
retained archive and actual executable check. A retrospective manifest is **not** a
compiler-input attestation, and Apple does not expose the original archive hash in
these checks. The archive was development-signed; automatic export re-signed for
distribution and Apple accepted it. Independent signature inspection of that exported
distribution package remains unperformed. No certificate/email/key contents are committed.

The initial consent screenshot has existing floating-tab overlap. Source permits
further scrolling; Reviewer tests decline, not Agree/persistence. This is an explicit
existing UX/testing gap, not evidence that capture changes broke consent or that full
consent behavior passed. Physical AR, actual room JPEGs/poses, sustained memory/thermal
behavior and reconstructed-room viewing cannot be established by this simulator gate.

Next physical step: install18 through TestFlight, capture an owned/cleared room, stop
and save, and inspect/export the local capture. Do not delete old attempts to test it.
No Phase B–E spatial product work starts before the owner's real-room experiment.
