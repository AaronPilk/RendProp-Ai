# Internal TestFlight 1.0.3 (30)

**Available in the existing internal Rendprop team TestFlight group**, verified September 21, 2026 at 23:12:56 UTC. Upload completed at 23:10:32 UTC. This document must not be read as evidence of physical room capture or reconstruction quality.

Apple build ID: `dbea1033-36c5-44c3-96a7-32b3ed55741a`. Apple reports `VALID`, `INTERNAL_ONLY`, `IN_BETA_TESTING`, not expired, and confirms the exact build belongs to the existing internal group. App Store version states and attached builds match the pre-upload snapshot. No tester invitation or group-membership write was needed. Read-only availability receipt: `/Users/pilksclaes/LocalRendpropAudits/testflight-20260921/testflight-ready.json`.

## Artifact and scope

- App: Rendprop, `com.rendprop.app`, existing App Store Connect app `6808982413`.
- Source: `0c02f76ae197c58333a22657e1dde78959c1c45a`, branch `release/spatial-testflight-20260921`.
- Base: audited capture repair `ddd636788da4db90a286ca9ffeb4e7f6ccb94975`, including call/recovery fixes at `1ed5009`.
- Explicit `RendpropSpatialTestFlight` Release scheme, freshly generated from the current YAML. Its 99 app source files match the current normal target and retain the audited source unchanged.
- Version `1.0.3`, build `30`, iOS 16 minimum, existing bundle/team and entitlement configuration.
- Export enforces `testFlightInternalTestingOnly=true` and `manageAppVersionAndBuildNumber=false`. No production App Store attachment or submission was requested.

The overlay enables Settings → TestFlight lab → Spatial capture (TestFlight). It records and exports a local scan without enabling spatial processing. The existing wrapper provides no upload/generation callback. The normal feature remains controlled by its existing capability gate.

The user explicitly authorized this TestFlight upload on September 21, superseding the earlier App Store Connect prohibition for this operation. No new testers were invited, no backend flags or migrations were changed, and no GPU was allocated.

## Verification performed

- Release simulator build-for-testing: passed, 133.25 seconds.
- Three focused capture-lab navigation tests: passed, zero skips, 86.14 seconds. The expected three screenshots were present and visually inspected. These cover entry, disclosure, idle/unsupported controls, saved-library navigation and exit; they do not verify physical AR or Files export.
- Signed generic iPhone Release archive: passed, 78.31 seconds.
- Actual archive verified for version/build, bundled resources, minimum OS, capture-lab markers, excluded secrets/scripts and strict code signature.
- One upload attempt: exit 0, 181.95 seconds. Xcode reported “Uploaded package is processing” and “Upload succeeded.” No repeat dispatch or automatic renumbering occurred.
- Independent review verified the regenerated source set and distribution helper. Exception handling was corrected before signing/upload to avoid printing credential paths or signing command arguments.

Archive executable SHA-256: `23905a422e4198fec48adc8fc1785db58c630335cca52e373cb33978cf6ae21c`.

All local evidence, including the source-bound commands, archive manifest, sanitized signing/upload logs and Apple pre/postflight snapshots, is in `/Users/pilksclaes/LocalRendpropAudits/testflight-20260921/`. The original Claude checkout and its existing uncommitted project changes remain untouched.

## Phone test

Install build 30 through the existing Rendprop TestFlight app. Open Settings → TestFlight lab → Spatial capture (TestFlight), then Start new room. Move slowly between overlapping viewpoints in a well-lit room, including revisits, door frames and a readable label. Aim for 300–350 photos and stop before the 400-photo/ten-minute ceiling.

Use Stop and save, allow saving to finish, then Export completed capture to Files/iCloud Drive. Transfer the entire saved capture folder for the next reconstruction experiment. Actual camera capture, sustained recording, interruptions and export need the physical iPhone.

The 3D quality goal remains incomplete. Existing reconstructions were rejected, and no winning runtime profile was enabled by this TestFlight delivery.
