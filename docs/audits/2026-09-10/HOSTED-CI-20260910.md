# Hosted CI execution — September10,2026

This report distinguishes GitHub-hosted tests from the local receipts and from
production deployment. No workflow below submits to Apple or deploys a service.

## First actual hosted run: overall FAILURE

Command: `gh workflow run ci.yml --repo AaronPilk/RendProp-Ai --ref audit/full-regression-20260910`.
GitHub run: [34544905277](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34544905277).
Exact source: **`68f39a24c9d685cc198dee3c899bf15a672716cd`**.
Finished September10 at20:05 Eastern. Job outcomes were read back, not inferred
from the dispatch returning a URL.

| Job | Actual outcome |
| --- | --- |
| Offline audit evidence and consent source gates |SUCCESS |
| Web parity/design-token contracts |SUCCESS |
| iOS static gates |SUCCESS; not an Xcode or device run |
| Cloudflare Worker |SUCCESS: clean npm ci, typecheck, actual tests, dry-run bundle and production dependency audit |
| Offline style policy |SUCCESS |
| Python worker |SUCCESS including HDR command; detailed media/log verification is being recorded separately |
| Supabase edge functions |FAILURE in test typechecking after entrypoint checks passed; no passing hosted test count claimed |
| Existing Supabase migration job |FAILURE at invariants; subsequent replay steps skipped |
| New disposable PostgreSQL job |FAILURE; exact first failing step/cause under review |
| Secret scan |FAILURE; exact action/setup versus finding classification under review, no secret values reproduced |

Confirmed edge failure: `tools/audit/uploads_publication_test.ts:166`, TS2322,
`Timeout` not assignable to `number`. The declaration assumed the browser-only
numeric return from `setTimeout`; this CI program's types return a Node-style
Timeout. GitHub used Deno2.9.6. The local Deno2.7.13 run still passes598; it does
**not** reproduce that version's type environment. Repair must use the actual
timer return type, not disable typechecking, skip the test or remove the timeout.

Other failures are not automatically attributed to the known197/198 invariant
until their logs show it. This report will be updated with the exact database
and scan classifications, the bounded fixes and a fresh hosted rerun. A green
worker subjob does not make this full workflow green.
