# Hosted CI execution — September10,2026

This report distinguishes GitHub-hosted tests from the local receipts and from
production deployment. No workflow below submits to Apple or deploys a service.

## First actual hosted run: overall FAILURE

Command: `gh workflow run ci.yml --repo AaronPilk/RendProp-Ai --ref audit/full-regression-20260910`.
GitHub run: [34544905277](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34544905277).
Exact source: **`68f39a24c9d685cc198dee3c899bf15a672716cd`**.
Finished September10 at20:05 Eastern. Job outcomes were read back, not inferred
from the dispatch returning a URL.

| Job | Actual outcome |
| --- | --- |
| Offline audit evidence and consent source gates |SUCCESS |
| Web parity/design-token contracts |SUCCESS |
| iOS static gates |SUCCESS; not an Xcode or device run |
| Cloudflare Worker |SUCCESS: clean npm ci, typecheck, actual tests, dry-run bundle and production dependency audit |
| Offline style policy |SUCCESS |
| Python worker |SUCCESS:199 checks/tests including19 actual HDR assertions; see HOSTED-WORKER-VERIFICATION.md |
| Supabase edge functions |FAILURE in test typechecking after entrypoint checks passed; no passing hosted test count claimed |
| Existing Supabase migration job |FAILURE:198 registered invariants, sole false row155 (AI headroom); subsequent replay steps skipped |
| New disposable PostgreSQL job |FAILURE only at final acceptance after publication/replay/negative controls and cluster stop; exact receipt reviewed separately |
| Secret scan |FAILURE: scanner executed and reported6 findings, not a setup/license error; value-free classification reviewed separately |

Confirmed edge failure: `tools/audit/uploads_publication_test.ts:166`, TS2322,
`Timeout` not assignable to `number`. The declaration assumed the browser-only
numeric return from `setTimeout`; this CI program's types return a Node-style
Timeout. GitHub used Deno2.9.6. The local Deno2.7.13 run still passes598; it does
**not** reproduce that version's type environment. Repair must use the actual
timer return type, not disable typechecking, skip the test or remove the timeout.

The two database logs have now been read: the owned-cluster job completed its
positive/negative fixtures and stop, then correctly returned1 for red invariant
acceptance. Artifact upload succeeded. The existing service-database job stopped
at the same invariant and did not run replay. Do not attribute unexecuted stages
to that older job merely because the new separate job ran them.

Detailed successful hosted media evidence:
[`HOSTED-WORKER-VERIFICATION.md`](HOSTED-WORKER-VERIFICATION.md). It records19
nonempty HDR assertions, the deliberate missing-binary negative control,
184.28KiB dry-run bundle,1,848 host assertions/12 self-tests and zero production
dependency advisories. This is not a deployed-bundle or real-camera claim.

## Second hosted run: the type fix exposes an actual fixture runtime failure

Run [34545436453](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34545436453)
used **`0051924a0193205733550584856a3f728022af56`** and finished overall FAILURE:
the same six jobs passed and four failed. Root's local Deno2.7.13 receipt on
that clean source still reports598 passing tests and21 entrypoint checks:
`/tmp/rendprop-edge-audit-fdr6937k/receipt.json`.

The timer repair correctly removed TS2322. Hosted edge tests then actually ran:
**569 passed,29 failed**. The failures are `Invalid property descriptor. Cannot
both specify accessors and a value or writable attribute`. The test fixtures
spread the original Deno.serve descriptor and add `value`; Deno2.9.6 exposes
serve through an accessor, making that replacement invalid JavaScript. This is
a separate fixture portability defect, not29 demonstrated upload product bugs.
Both interception sites are being repaired and will be rerun without removing
the actual-handler tests or their timeouts/assertions. See the timer handoff
for why the local runtime alone did not reproduce the first CI failure.

No production operation or Apple change occurred. No overall green is claimed.

## Third hosted run: edge repaired and actually passing

Run [34546017429](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34546017429)
used **`7b60ada5cbfcadd22d97342af233d1fb33eb2489`**. Overall result remains
FAILURE: **seven jobs pass; the two database jobs and scanner remain red**.
Root read the completed run and the actual edge log, not only the dispatch URL.

[Edge job103098683585](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34546017429/job/103098683585)
now passes all21 entrypoint checks and **602 tests,0 failed**, with no ignored
tests in the summary. The added four actual-route tests exercise both data and
accessor descriptor shapes and restoration after success/failure. The timer
and descriptor failures are therefore repaired under the actual hosted Deno
environment, not merely under the older local runtime. Root's same-source
local gate also passed602/0/0 plus21 checks and the deliberately broken
Turnstile control: `/tmp/rendprop-edge-audit-lszgf9yw/receipt.json`.

The worker/host/style/web/iOS-static/audit jobs pass again. Their source files
are unchanged from the detailed68f39a2 evidence. The latest database and scanner
jobs remain failures; detailed independently verified original failure reasons
are in [`CI-RUN-34544905277.md`](CI-RUN-34544905277.md). That report records actual
PostgreSQL16.15 fixtures/replay/artifact validation and value-free classification
of all six historical scanner hits. No broad allowlist or budget change was
used to force a green workflow.

## Fourth hosted run: integrated lead-form repair passes

Run [34546654166](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34546654166)
used **`a9b13b1f1b66fee1f52fdd566e7de82f8b272b3b`**. It completed with the same
**seven passing/three failing jobs, overall FAILURE**. Root read completed job
states and the actual host/edge/footer/DB/scan summaries.

[Host job103100643656](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34546654166/job/103100643656)
passes clean npm ci, typecheck and the full2,266 assertions plus12 self-tests:
557 unbranded,584 route,707 upstream and418 emitted-lead-form assertions.
The new form suite runs31 cases,0 skips,0 real network requests. Dry-run bundle
is **186.08KiB, gzip57.92KiB**, and explicitly exits without deployment.
Production npm audit reports **0 vulnerabilities**. Local root separately ran
the2-file asset gate; it is not silently attributed to this hosted job.

[Edge job103100643519](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34546654166/job/103100643519)
again reports **602 passed,0 failed**, after its entrypoint checks. Worker,
style, web contracts, iOS static and audit-harness jobs also succeed. Worker
source is unchanged from the separately counted199-check hosted evidence.

The disposable database job again executes its publication/restore controls
and `stop: exit=0`, then ends with `Database assertions remain red`; receipt
path on that runner is `/tmp/rendprop-db-audit-t9e9d7o0/receipt.json`. The scanner
reports **six findings over237 commits**; no values were printed during root's
summary extraction. Exact earlier value-free classifications remain in the
CI-run report. No policy weakening, credential mutation, production service
change or Apple operation was used to alter these remaining gates.
