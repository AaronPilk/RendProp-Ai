# CI run 34544905277: database and secret-scan findings

Review date: 2026-09-10 America/New_York (job timestamps are 2026-09-11 UTC). Source: `68f39a24c9d685cc198dee3c899bf15a672716cd`. Read-only investigation on `fix/ci-disposable-pg-run34544905277`; **no CI/runner repair was warranted** by these three jobs. This commit adds evidence only, without modifying the workflow, database guards, fixture counts, secret rules, allowlists, history, app or production configuration.

## Disposable PostgreSQL: infrastructure worked; invariant correctly failed

[Job 103095319072](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34544905277/job/103095319072) installed the selected binaries successfully and executed the unchanged runner. Actual version: **PostgreSQL 16.15, Ubuntu package 16.15-1.pgdg24.04+2**. It verified its owned data-directory/database identity and empty TCP listen address. It did not use the separate `rendprop` TCP service.

| Executed operation | Actual result |
| --- | --- |
| All 38 migrations and 30 replayable migrations | Exit 0 for each command |
| Initial and replayed invariant suites | 198 assertions each; 197 pass, the same single failure on both passes; psql exit 3 |
| Sole false assertion | 155: `each astra ceiling clears its route's visible answer and stays under the code clamp` |
| Worker publication positive/restored fixture | 22 checks on each of four runs; every run exit 0 |
| Worker ownership, uploaded-video and scalar guard-removal controls | Each deliberately broken guard rejected for its intended failure; exit 3; source restored and positives rerun |
| Upload publication positive/restored fixture | 19 checks on each of two runs; exit 0 |
| Upload guard-removal control | Intended rejection detected, exit 3; source restored |
| Paid-route controls / deliberately corrupted team entitlement | Six paid-gate outcomes completed; entitlement corruption rejected with exit 3 |
| Exact owned cluster shutdown | Exit 0; receipt `clusterStopped=true` |
| Runner acceptance | `accepted=false`; final `Database assertions remain red` error and process exit 1 |
| Failure artifact step | Succeeded; artifact `disposable-database-regression-1`, id `10178649448` |

The final red status is **not** a PostgreSQL install, dirty-checkout, path, socket, publication, cleanup, or artifact-upload defect. The retained product/configuration mismatch must not be converted to success. Its meaning and limits remain those documented in [DATABASE-EXECUTED-RESULTS.md](DATABASE-EXECUTED-RESULTS.md): the seeded agent-reel ceiling does not exceed its requested visible-answer allowance; this is not evidence that every real response is truncated.

Downloaded artifact: `/tmp/rendprop-ci-run34544905277.SPK0XU/database-artifact/rendprop-db-audit-ewye8z92/receipt.json`. It contains 100 files: one receipt and 99 logs, including the server log; **no cluster or raw database files**. Independently checked 166 assertions against the downloaded evidence, including both actual 198-row SQL tables, all 98 receipt-command log hashes, and all 45 source hashes against the exact committed source. All passed, exit 0. This was verification of the already completed remote run, not another database execution.

## Existing migrations job: also a real invariant failure

[Job 103095319090](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34544905277/job/103095319090) completed container setup, bootstrap and all migrations. Its actual initial output contains 198 rows and exactly one false row: the same assertion 155. The `Invariants` step exited 3 at `services/supabase/tests/invariants.sql:1876`. Its subsequent replay and second invariant steps were **skipped** by normal fail-fast behavior. Do not claim this job proved replay; the separate disposable job did prove replay while preserving the red result.

## Secret scan: scanner findings, not license/setup failure

[Job 103095319018](https://github.com/AaronPilk/RendProp-Ai/actions/runs/34544905277/job/103095319018) ran the scanner against **221 commits** and reported **six findings**. Checkout succeeded. License-related text in the log is not the reason to label this an unexecuted scan: the scanner ran and produced findings and the `gitleaks-results.sarif` artifact (id `10178644848`). The command included redaction and all six `Secret:` fields were redacted. No detected value is reproduced in this document.

Locations below are the scanner's **historical** locations, not assertions that the same line number currently identifies a value. The first five are from commit `bcba8040bfa10eb0f199e83aa2787fe0f15d5ba9`; the Config finding is from `4e4776dbdd8bf3e7408e805c2d313e1b46087b76`.

| Historical file:line | Rule | Bounded source classification / remaining decision |
| --- | --- | --- |
| `docs/handoff/launch-P2.md:517` | `curl-auth-header` | A documented curl request with an Authorization header. Its detected header was not established to be a safe placeholder by this value-free inspection. **Unresolved historical credential/placeholder triage**, not an asserted compromise. An authorized owner must inspect privately; if genuine, verify rotation/revocation and dependent consumers without publishing the value. |
| `services/supabase/functions/_shared/applejws.test.ts:924` | `generic-api-key` | Fixed synthetic UUID used as an `appAccountToken` test input and compared with the decoded result; source evidence supports a fixture, not an API key. |
| `services/supabase/functions/apple-subscriptions/notify.test.ts:293` | `generic-api-key` | Synthetic UUID in the `appAccountToken` input to a notification-summary test. The test checks exclusion of sensitive fields from stored summary data; not evidence of an issued API credential. |
| `services/supabase/functions/admin/probe.test.ts:178` | `private-key` | `freshP8Pem()` calls WebCrypto `generateKey` and `exportKey`, base64-encodes the fresh private key and interpolates its body into a PEM at test runtime. The source match is a test PEM-construction template, not a persisted private-key body. |
| `services/supabase/functions/admin/probe.test.ts:275` | `jwt` | A `jwtish` fixture exercises `sanitize`; assertions require long JWT segments and bearer content to be absent from output. This is a redaction-test input, not evidence that it authenticates to a service. No token was sent anywhere. |
| `apps/ios/Rendprop/Config.swift:33` | `jwt` | Source identifies it as the bundled `supabaseAnonKey`; its decoded, **unverified** payload claims role `anon`, not `service_role`, and the same token remains in current source. This is intended client configuration, not evidence of an administrative credential. Signature, current validity, project configuration and RLS safety were **not** verified. Owner must confirm that public-client use is intended; do not declare the whole secret scan clean from its variable name alone. |

No broad rule suppression, allowlist, baseline exception, history rewrite or credential rotation was performed. Historical findings remain in this full-history scan even if today's files are edited. The evidence above does not establish that any privileged live credential was leaked, nor does it resolve the historical curl finding.

Possible future narrow fixture cleanup: the two `appAccountToken` tests can use an unmistakably synthetic, valid low-entropy UUID such as `00000000-0000-4000-8000-000000000001`. Bind each test's input and equality/exclusion assertions to the same named fixture; do not change only the input and leave an assertion checking the old value. This would require the actual tests to pass and a scanner check, and would **not** remove the historical findings. No such edit is proposed as a fix for this CI run. Do not remove JWT-shaped sanitizer inputs or runtime PEM construction merely to avoid their detector rules; that can weaken the tests' purpose. Any precise historical exception needs owner review, not a blanket test-directory exemption.

## Scope and handoff

The three exact job logs were fetched read-only into `/tmp/rendprop-ci-run34544905277.SPK0XU`, a private temporary directory; raw logs are not committed. Secret analysis reported structural classifications rather than values. One attempted literal-masking source excerpt proved unreliable and was abandoned; subsequent source checks emitted only booleans/identifiers. This report contains no detected literals, credentials or customer data.

Final local checks passed: six finding-value exclusions against this report, absence of token-shaped text, whitespace validation, and confirmation that no tracked code changed. `git diff --check` exited 0. These are evidence/document checks, not a replacement secret scan or a new SQL run.

The workflow at `.github/workflows/ci.yml:243–288` did what it was designed to do: preserve a real red database result and its seven-day receipt/log artifact. The existing secret-scan job remains blocking at its pinned action; no passing security result is claimed. No rerun, installation, database/server start, provider call, Apple operation, deployment or production mutation was performed in this investigation. Root owns later CI integration/runs and any credential or product-policy decisions.
