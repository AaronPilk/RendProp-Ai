# Handoff to Codex — 13 Sep 2026

Read this before rebasing. Everything below was read back from production or
App Store Connect today; nothing is inferred from an older report.

## Migration numbers have moved again

Production now records **51** migrations. Taken since your last handoff:

| number | what |
|---|---|
| 0044 | plan_rework_and_industry_trial |
| **0045** | **STILL RESERVED FOR YOU — `upload_explicit_restart`** |
| 0046 | commercial_telemetry |
| 0047 | lifecycle_notifications |
| 0048 | brokerage |
| 0049 | notification_drain_schedule |
| 0050 | brokerage_contracts |
| 0051 | brokerage_price_floor |

Your `0042_upload_explicit_restart.sql` still needs renaming to **0045**. That
has not changed and nothing else has taken the number.

## The branch tip

`claude/appstore-hardening-20260912` is at `44fa20b`. `feat/spatial-release-integration-20260911`
was still 0 commits ahead of `d63b64d` when I checked, so nothing of yours is
waiting to be integrated — rebase onto `44fa20b`.

## What is live in production that was not before

- **Notifications end to end.** Migration 0047 built the outbox; 0049 added the
  missing caller (`notification_drain()` on a one-minute pg_cron via pg_net,
  service-role key read from Vault). Both channels are proven on the real cron:
  email through Resend at 15:14 UTC, APNs push at 15:23 with Apple's own
  apns-id on the log row. `functions/notify` is service-role only by design.
- **A brokerage plan.** `plan_entitlements.team.seats` was 2, so 0033 refused
  the third agent into any org with RP402. 0050 adds `brokerage_contracts`
  (one row per signed deal, per-seat allowances pooled across the office) and
  teaches `effective_plan` / `org_seats_allowed` / `org_entitlement` to read it.
  0051 sets the list ladder ($149/$119/$89) and a $50/seat floor enforced
  inside `set_brokerage_contract`.
- **`_shared/router.ts` PLAN_ORDER now includes `brokerage`.** It must stay
  there: `planRank()` returns 0 ("free", the most restrictive) for an
  unrecognised plan, so omitting it silently routes a paying brokerage to the
  cheapest model and refuses anything gated above free. `coach/index.ts
  KNOWN_PLANS` has the same list.
- **`events/schema.ts scrubMeta` passes a bare version number through.** The
  phone rule matched any three-component version with a build number, so
  `1.0.2 (23)` was stored as `[redacted])` and every version-keyed analytic
  (`admin_cohorts`, `admin_churn`) was grouping on garbage. The pass-through is
  a whole-string anchored match — do not widen it.

## App Store

Version **1.0.1 carries build 24** and went back into review at
2026-09-13T16:39:07Z on submission `1f8de50f`. Build 22's submission was
cancelled to make room, because App Store Connect only allows one version in
preparation at a time (creating 1.0.2 returns HTTP 409 while another version is
queued). 1.0 (build 16) is still READY_FOR_SALE. The five subscriptions came
back APPROVED and are not part of this submission.

**Do not touch anything in App Store Connect while that is in the queue.**

## Two API facts worth keeping

- `reviewSubmissionItems` exposes `appStoreVersion`, `subscriptionVersion` and
  `subscriptionGroupVersion` — NOT `subscription`/`subscriptionGroup`. POSTing
  an item keyed `subscription` works; reading the collection back with that
  field name is a 400.
- `xcodebuild archive` needs `-authenticationKeyPath/-authenticationKeyID/-authenticationKeyIssuerID`
  now that the Push Notifications entitlement forces a fresh provisioning
  profile — not only `-exportArchive`.
