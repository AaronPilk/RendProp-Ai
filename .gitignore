.DS_Store
node_modules/
*.log
.env
.env.*
!.env.example
dist/
build/
DerivedData/
*.xcuserstate

# iOS dev secrets (never commit)
apps/ios/Rendprop/Resources/Secrets.plist

# Python
__pycache__/
*.pyc
.venv/
venv/

# Supabase / edge local
services/edge/tour-host/node_modules/
.wrangler/
graphify-out/

# Supabase deploy staging dir (deploy-functions.sh builds + deletes it; never commit)
services/supabase/.deploy/
services/supabase/functions/deno.json
services/supabase/functions/deno.lock

# Belt-and-braces for the secret .env the audit found in a shared snapshot
# (F-G-12). `.env` above already matches at every depth; these are explicit so a
# future `!`-rule can't accidentally un-ignore them.
services/pipeline/.env
services/worker/.env
