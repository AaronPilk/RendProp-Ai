# Rendprop render worker deps.
# The AI-enhancement PIPELINE (services/pipeline) is stdlib-only and needs no
# install — the worker just imports it. These are the worker's own needs:
#
# PINNED to exact versions (audit round 4): floating `>=` ranges made the image
# non-reproducible — two builds of the same commit could ship different library
# versions, including ones with fresh CVEs. Bump these deliberately, then
# rebuild and re-test.

boto3==1.35.99        # R2 (S3 API): download capture, upload mp4/poster, presign
botocore==1.35.99     # pinned with boto3 — they move in lockstep
requests==2.32.3      # Supabase PostgREST + Cloudflare Stream REST
urllib3==2.2.3        # requests' transport; pinned so CVE bumps are deliberate

# System binaries (NOT pip): ffmpeg + ffprobe are REQUIRED (the encode + poster +
# the pipeline's keyframe extraction). The Dockerfile installs them. Locally:
#   macOS:  brew install ffmpeg
#   debian: apt-get install -y ffmpeg
#
# ffmpeg/ffprobe run under wall-clock timeouts and a max source duration —
# see ffmpeg_render.py (FFPROBE_TIMEOUT_S / FFMPEG_TIMEOUT_S / MAX_SOURCE_SECONDS).
