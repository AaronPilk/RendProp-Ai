import assert from "node:assert/strict";
import { mkdtemp, readFile, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join, resolve, extname } from "node:path";
import { createServer } from "node:http";
import { build } from "vite";
import { chromium, expect } from "@playwright/test";
const root=resolve(import.meta.dirname,".."),artifacts=await mkdtemp(join(tmpdir(),"rendprop-reel-media-browser-")),dist=join(artifacts,"dist");
const receipt={proof:"Real CloudEditor and media picker, using isolated uploaded source fixtures; no paid requests or live account",checks:[],errors:[],externalRequests:[]};
let server,browser;
try {
  await build({configFile:false,root,publicDir:false,logLevel:"error",build:{outDir:dist,rollupOptions:{input:join(root,"tests/cloud-editor-fixture.html")}}});
  server=createServer(async(req,res)=>{const path=resolve(dist,`.${new URL(req.url,"http://localhost").pathname}`);if(!path.startsWith(`${dist}/`))return res.writeHead(400).end();try{res.setHeader("Content-Type",({".html":"text/html",".js":"application/javascript",".css":"text/css"})[extname(path)]??"application/octet-stream");res.end(await readFile(path));}catch{res.writeHead(404).end();}});
  await new Promise(done=>server.listen(0,"127.0.0.1",done));const origin=`http://127.0.0.1:${server.address().port}`;
  browser=await chromium.launch({headless:true,executablePath:process.env.STUDIO_BROWSER_EXECUTABLE});const context=await browser.newContext({viewport:{width:1440,height:1000},serviceWorkers:"block"});
  await context.route("**/*",route=>{const url=new URL(route.request().url());if(url.origin===origin||url.protocol==="blob:")return route.continue();receipt.externalRequests.push(url.href);return route.abort();});
  const page=await context.newPage();page.on("pageerror",error=>receipt.errors.push(error.message));page.setDefaultTimeout(12000);
  await page.goto(`${origin}/tests/cloud-editor-fixture.html?listing=20000000-0000-4000-8000-000000000003`);await expect(page.getByLabel("Title overlay",{exact:true})).toBeVisible();
  await page.evaluate(async()=>{
    const org="10000000-0000-4000-8000-000000000001",a="20000000-0000-4000-8000-000000000002",b="20000000-0000-4000-8000-000000000003";
    const canvas=document.createElement("canvas");canvas.width=640;canvas.height=360;canvas.getContext("2d").fillStyle="#7c3aed";canvas.getContext("2d").fillRect(0,0,640,360);
    const base64=canvas.toDataURL("image/png").split(",")[1],bytes=Uint8Array.from(atob(base64),c=>c.charCodeAt(0)),sha256=[...new Uint8Array(await crypto.subtle.digest("SHA-256",bytes))].map(n=>n.toString(16).padStart(2,"0")).join("");
    const source={name:"phone-photo.png",size:bytes.length,lastModified:0,sha256,kind:"image",width:640,height:360,duration:0};
    const assets=Array.from({length:52},(_,index)=>{const id=`30000000-0000-4000-8000-${String(index+1).padStart(12,"0")}`,listing_id=index===51?b:a;return{id,listing_id,storage_key:`uploads/${org}/${listing_id}/${id}.png`,content_type:"image/png",kind:"photo",uploaded:true,base64,bytes:bytes.length};});
    const draft={schema:1,id:crypto.randomUUID(),revision:1,ratio:"9:16",title:"Office saved story",audio:"original",clips:[{id:crypto.randomUUID(),source,start:0,end:3,caption:"Already in my reel",focusX:.5,focusY:.5}]};
    const edit={key:`edit:${b}`,kind:"edit",listing_id:b,revision:1,updated_at:new Date().toISOString(),payload:{draft,listingId:b,sources:[{sha256,assetId:assets[51].id,listingId:b}]}};
    localStorage.clear();localStorage.setItem("fixture-cloud",JSON.stringify({assets,documents:{[`edit:${b}`]:edit},tickets:0,puts:0,writes:0}));
  });
  await page.reload();await expect(page.getByLabel("Title overlay",{exact:true})).toHaveValue("Office saved story");await expect(page.getByRole("button",{name:/Select clip 1:/})).toBeVisible();
  await expect(page.getByRole("button",{name:"Reselect original file",exact:true})).toHaveCount(0);
  const before=await page.evaluate(()=>window.cloudFixture.snapshot());
  await page.evaluate(()=>window.cloudFixture.requestReelEntry("home-card-request","20000000-0000-4000-8000-000000000002"));
  const picker=page.getByRole("region",{name:"Choose property photos and video",exact:true});
  await expect(picker).toBeVisible();await expect(picker.getByRole("checkbox")).toHaveCount(50);
  await expect(page.getByLabel("Title overlay",{exact:true})).toHaveValue("");
  await expect(page.getByLabel("Property reel",{exact:true})).toHaveValue("20000000-0000-4000-8000-000000000002");
  assert.deepEqual((await page.evaluate(()=>window.cloudFixture.snapshot())).documents["edit:20000000-0000-4000-8000-000000000003"],before.documents["edit:20000000-0000-4000-8000-000000000003"]);
  await picker.getByRole("button",{name:"Close media picker",exact:true}).click();await expect(picker).toHaveCount(0);
  await page.evaluate(()=>window.cloudFixture.requestReelEntry("home-card-request","20000000-0000-4000-8000-000000000002"));await page.waitForTimeout(250);await expect(picker).toHaveCount(0);
  assert.deepEqual((await page.evaluate(()=>window.cloudFixture.snapshot())).documents["edit:20000000-0000-4000-8000-000000000003"],before.documents["edit:20000000-0000-4000-8000-000000000003"]);
  receipt.checks.push("A home-card request opens its own separate property reel once; opening, closing and replay leave the other property draft untouched");
  await page.evaluate(()=>window.cloudFixture.requestReelEntry("page-two-request","20000000-0000-4000-8000-000000000002"));
  await expect(picker.getByRole("checkbox")).toHaveCount(50);await picker.getByRole("button",{name:"Load more files",exact:true}).click();await expect(picker.getByRole("checkbox")).toHaveCount(51);
  await picker.getByRole("checkbox").nth(50).check();
  await picker.getByRole("button",{name:"Add 1 file to reel",exact:true}).click();
  await expect(page.getByRole("button",{name:/Select clip 1:/})).toBeVisible();
  await expect.poll(async()=>(await page.evaluate(()=>window.cloudFixture.snapshot())).documents["edit:20000000-0000-4000-8000-000000000002"]?.payload.draft.clips.length).toBe(1);
  assert.deepEqual((await page.evaluate(()=>window.cloudFixture.snapshot())).documents["edit:20000000-0000-4000-8000-000000000003"],before.documents["edit:20000000-0000-4000-8000-000000000003"]);
  receipt.checks.push("The real 50-row media pagination reaches photo51 and appends it only to that property's separate reel");
  await page.evaluate(()=>window.cloudFixture.requestReelEntry("same-property-request","20000000-0000-4000-8000-000000000003"));
  await expect(picker.getByRole("checkbox")).toHaveCount(1);await picker.getByRole("checkbox").check();await picker.getByRole("button",{name:"Add 1 file to reel",exact:true}).click();
  await expect(page.getByRole("button",{name:/Select clip 2:/})).toBeVisible();
  await expect.poll(async()=>(await page.evaluate(()=>window.cloudFixture.snapshot())).documents["edit:20000000-0000-4000-8000-000000000003"].payload.draft.clips.length).toBe(2);
  const after=await page.evaluate(()=>window.cloudFixture.snapshot());assert.equal(after.tickets,before.tickets);assert.equal(after.puts,before.puts);assert.equal(after.documents["edit:20000000-0000-4000-8000-000000000003"].payload.listingId,before.documents["edit:20000000-0000-4000-8000-000000000003"].payload.listingId);assert.equal(after.documents["edit:20000000-0000-4000-8000-000000000003"].payload.draft.title,"Office saved story");assert.equal(after.documents["edit:20000000-0000-4000-8000-000000000003"].payload.sources[0].assetId,before.documents["edit:20000000-0000-4000-8000-000000000003"].payload.sources[0].assetId);
  receipt.checks.push("Same-property phone source appends to the saved sequence with its actual asset identity; no new upload ticket, bytes or generated media");
  await page.getByRole("button",{name:"Create a voiceover",exact:true}).click();await expect(page.getByText("Open voiceover for 20000000-0000-4000-8000-000000000003",{exact:true})).toBeVisible();
  await page.getByRole("button",{name:"Help plan my reel",exact:true}).click();await expect(page.getByText("Open shot-plans for 20000000-0000-4000-8000-000000000003",{exact:true})).toBeVisible();
  receipt.checks.push("Three-step guide sends voiceover and shot-plan actions to the existing edit’s actual property");
  await page.getByRole("button",{name:"Choose property photos & video",exact:true}).click();await expect(picker.getByRole("checkbox")).toHaveCount(1);
  await page.screenshot({path:join(artifacts,"reel-media-desktop.png"),fullPage:true});
  await page.setViewportSize({width:390,height:844});await expect(picker).toBeVisible();await page.screenshot({path:join(artifacts,"reel-media-mobile.png"),fullPage:true});assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+2));
  receipt.checks.push("Desktop and 390px picker/guide stay readable without document overflow");
  assert.equal(receipt.errors.length,0);assert.equal(receipt.externalRequests.length,0);
  await writeFile(join(artifacts,"receipt.json"),JSON.stringify({...receipt,status:"passed",artifacts},null,2));console.log(JSON.stringify({...receipt,status:"passed",artifacts},null,2));
} catch(error) {console.error(JSON.stringify({...receipt,artifacts,error:error.stack},null,2));throw error;} finally {await browser?.close();await new Promise(done=>server?.close(done)??done());}
